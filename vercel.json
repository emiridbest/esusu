{
  "version": 2,
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "npm install --workspace=frontend --workspace=backend",
  "outputDirectory": "frontend/.next",
  "functions": {
    "app/api/**": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
